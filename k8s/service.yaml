apiVersion: v1
kind: Service
metadata:
  name: awoke-student-api-service
  namespace: default
  labels:
    app: awoke-student-api
spec:
  type: ClusterIP
  selector:
    app: awoke-student-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8181